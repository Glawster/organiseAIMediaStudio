# organiseAIMediaStudio Frontend

## Getting Started

1. Run `npm install` to install dependencies
2. Run `npm start` for the dev server.

This project uses React and Material UI for a modern interface.